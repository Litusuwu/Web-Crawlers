# TikTok Scraper — Environment Configuration
# Copy this file to .env and fill in the values

# ── Proxy Configuration ──────────────────────────────────────────────────────
# Residential proxy is REQUIRED for TikTok scraping.
# Datacenter proxies will be instantly blocked.
#
# Format: protocol://user:password@host:port
# Example: http://user123:pass456@us.residential.proxy.io:8080
PROXY_URL=
PROXY_USERNAME=
PROXY_PASSWORD=
PROXY_HOST=
PROXY_PORT=

# Optional: SOCKS5 proxy for nodriver
# Format: socks5://user:password@host:port
SOCKS5_PROXY_URL=

# ── Browser Configuration ────────────────────────────────────────────────────
# Headless mode: "true" for headless, "false" for headed (debugging)
HEADLESS=true

# Browser viewport (should match common real resolutions)
VIEWPORT_WIDTH=1920
VIEWPORT_HEIGHT=1080

# User-Agent override (leave empty to use stealth default)
USER_AGENT=

# ── Scraping Configuration ───────────────────────────────────────────────────
# Minimum delay between requests (seconds)
MIN_REQUEST_DELAY=3
# Maximum delay between requests (seconds)
MAX_REQUEST_DELAY=10

# Maximum pages/scrolls per session before rotating
MAX_PAGES_PER_SESSION=5

# ── AWS Configuration (Lambda evaluation only) ───────────────────────────────
AWS_REGION=us-east-1
AWS_LAMBDA_FUNCTION_NAME=tiktok-scraper
AWS_S3_BUCKET=

# ── Logging ──────────────────────────────────────────────────────────────────
LOG_LEVEL=INFO
